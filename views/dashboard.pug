extends layout

block content
  section.box
    h2 Dashboard
    p Welcome back, #{currentUser.name}.
    p Your email: #{currentUser.email}

  section.box
    h2 Quick Info
    p Total services: #{serviceCount}
    p Account type: User
